language: cpp
sudo: required
dist: trusty
compiler:
- gcc
- clang
install:
    - if [ "$CXX" = "g++" ]; then export CXX="g++-6" CC="gcc-6"; fi
    - if [ "$CXX" = "clang++" ]; then export CXX="clang++-3.8" CC="clang-3.8"; fi
os:
- linux
- osx
script:
- cmake .
- make
- make check
- ./Engine_check
