language: cpp
sudo: required
dist: trusty

before_install:
- sudo apt-get update -qq
- sudo apt-get install libglm-dev libglfw-dev libglew-dev libsoil-dev

compiler:
- gcc
- clang
os:
- linux
script:
- cmake .
- make
- make check
- ./Engine_check
